# Development-only values. Copy this file to .env and replace values as needed.
# Never use these credentials outside an isolated local development environment.
MYSQL_PORT=3307
MYSQL_DATABASE=operationshub
MYSQL_USER=operationshub
MYSQL_PASSWORD=operationshub_dev_only
MYSQL_ROOT_PASSWORD=operationshub_root_dev_only
